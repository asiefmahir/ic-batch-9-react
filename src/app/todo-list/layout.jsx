export default function RootLayout({ children }) {
	return (
		<>
			{children}
			<ul>
				<li>Just for about sub/children paths</li>
				<li>Just for about sub/children paths</li>
			</ul>
		</>
	);
}
